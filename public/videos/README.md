# Hero video

Add your homepage hero video here as:

- `hero.mp4` — the background video (10-30s loop, muted, no audio needed, ~1920x1080, compressed for web)
- `hero-poster.jpg` — a still frame shown while the video loads

Until you add these files, the hero section falls back to a gradient
placeholder so the homepage still looks intentional.
